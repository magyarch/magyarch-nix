#!/usr/bin/env bash

# This file runs when a DM logs you into a graphical session.

#xrandr --dpi 144 >/dev/null 2>&1 &
#xrandr --output DP-2 --mode 2560x1440@144 --scale 1.25 >/dev/null 2>&1 &
#export GDK_SCALE=2

#sxhkd -c ~/.config/herbstluftwm/sxhkdrc &     # Simple X hotkey daemon for setting up keybinds
sxhkd -c ~/nixos-flake/local-packages/dwm/sxhkdrc &
#mpd &       # Music player daemon

xsetroot -cursor_name left_ptr &   # Change cursor from default x shape

xset r rate 300 25  2>&1 &	# Speed xrate up


setbg  2>&1 &

#pidof -q compfy || compfy --config $HOME/.config/compfy.conf &
#sxhkd -c ~/.config/qtile/sxhkdrc &
#(sleep 2s && $HOME/.config/polybar/launch.sh) &
picom --config $HOME/.config/picom.conf >/dev/null 2>&1 &
dunst >/dev/null 2>&1 &
                                # notification daemon

#unclutter &       # hide mouse pointer when idle

nm-applet &        # networkmanager applet/wifi,ethernet settings

#/usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 &

#zathura ~/.local/share/Magyarch-i3.pdf &
#sleep 8s && notify-send 'Nyomja meg a Super+F1-et a használati utasításért.' -t 10000 &

xset s off -dpms >/dev/null 2>&1 &
#blueman-applet &
#udiskie -a &
#redshift -l 47.162495:19.503304 -t 6000:3600 -g 0.8 -m randr -v &
#xrandr --output HDMI-0 --scale 2x2 --mode 1920x1080 --rotate left --output DP-0 --primary --mode 3840x2160 --pos 1080x0 --left-of HDMI-0 &
slstatus >/dev/null 2>&1 &
#stalonetray &
/nix/store/$(ls -la /nix/store | grep 'mate-polkit' | grep '4096' | awk '{print $9}' | sed -n '$p')/libexec/polkit-mate-authentication-agent-1 >/dev/null 2>&1 &

