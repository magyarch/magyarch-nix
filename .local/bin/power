#!/usr/bin/env bash

DIALOG_RESULT=$(echo -e 'Quit\nReboot\nPoweroff' | wofi --dmenu 'System')

case $DIALOG_RESULT in
        "Quit") qtile cmd-obj -o cmd -f shutdown ;;
        "Reboot") systemctl reboot ;;
        "Poweroff") systemctl poweroff ;;
        *) exit 0
esac
